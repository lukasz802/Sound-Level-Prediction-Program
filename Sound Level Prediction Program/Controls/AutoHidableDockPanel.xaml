<UserControl x:Class="Sound_Level_Prediction_Program.Controls.AutoHidableDockPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sound_Level_Prediction_Program.Controls"
             mc:Ignorable="d" x:Name="AHDockPanel"
             d:DesignHeight="450" d:DesignWidth="800"
             SnapsToDevicePixels="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}, Path=SnapsToDevicePixels}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height ="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" x:Name="MainPanelContent"
                        Content="{Binding ElementName=AHDockPanel, Path=MainContent}"/>
        <ContentControl Grid.Row="1" x:Name="CollapsedPanelContent"
                        Content="{Binding ElementName=AHDockPanel, Path=CollapsedContent}"/>
    </Grid>
</UserControl>
